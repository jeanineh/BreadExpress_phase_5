
<% if logged_in? && current_user.role?(:admin) %>
<h2>Available Items</h2>
  <%= render partial: "partials/index_structure", locals: {model_name: "item", 
                                                         primary: "active_items", 
                                                         secondary: "inactive_items",
                                                         sidebar: "partials/side_nav"} %>
<% end %>